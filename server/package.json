{
  "name": "worktodo-server",
  "version": "1.0.0",
  "private": true,
  "description": "WorkToDo backend — a single Express server + JSON file store, no accounts needed",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
